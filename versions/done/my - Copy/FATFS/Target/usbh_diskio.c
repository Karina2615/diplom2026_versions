/* usbh_diskio.c — USB Host disk I/O stub.
 * USB Host is not used in this project (SPI SD card is used instead).
 * File left empty intentionally to prevent build errors.
 * See FATFS/Target/spi_diskio.c for the active SD card driver.
 */
